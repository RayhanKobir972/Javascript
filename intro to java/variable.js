// vary + able = variable
/**
 * to declare a variable we need 5 steps
 * 1. var
 * 2.name
 * 3.assignment operator or equal(=) sign
 * 4.value
 * 5.semi-colon(;)
 */
// Neumeric
var price = 45;
var weight = 35;
var money = 1112;
// String
var person = "rayhan kobir";
var location = "parbatipur dinajpur"; 
// Boolean
var serious = true;
var isFullMarks = true;
var isSingle = false;

// variable name rules
/**
 * 1.variable name can not be any reserved word or keywords
 * 2.variable name has to be in one word. no space
 * 3.variable name can not have any quotation
 * 4.variable name can not starts with a number but can ends with a number
 * 5.how to use long name
 */